"""Kedro project settings for transfer_risk.

Defaults are used for this scaffold. kedro-mlflow registers its tracking hooks
automatically via plugin entry points (config in ``conf/base/mlflow.yml``), so no
explicit ``HOOKS`` entry is required here. See the Kedro docs to customise the
config loader, session store, or hooks as the project grows:
https://docs.kedro.org/en/stable/configure/configuration_basics/
"""
