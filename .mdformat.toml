# Keep each Markdown paragraph on a single line (no hard wrapping); diffs then change
# words, not line-rewraps. Read by `just fmt` and the mdformat pre-commit hook. refs/
# holds the reference papers' material, which we do not reformat.
wrap = "no"
exclude = ["refs/**"]
